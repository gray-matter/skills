# Accessibility touches (lightweight but high value)

- **`aria-live`** on modal error regions and polite status messages.
- **`role="status"`** / **`role="alert"`** where the semantics match (errors vs. selections).
- **Keyboard**: support Enter/Space on interactive cells where click is primary.
- **Selection state**: mirror visual selection with `aria-selected` when applicable.
